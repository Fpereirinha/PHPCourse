<?php
$anyComparation = 5 > 2;
$anyNBool = 5;
if (is_bool($anyComparation)) {
    echo "Yes, the variable is a bool. <br>";
} else {
    echo "No, it's not a boolean variable.";
}
if (is_bool($anyNBool)) {
    echo "Yes, the variable is a bool. <br>";
} else {
    echo "No, it's not a boolean variable.";
}
